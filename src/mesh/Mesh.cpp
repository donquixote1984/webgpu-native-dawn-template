#include "Mesh.h"
#include "MeshCreator.h"

/*
void MeshData::SetVertexType(VertexType type)
{
    m_VertexType = type;
    switch (type)
    {
    case VertexType::POSITION:
        m_Material.SetPipeline(*MeshPipeline::Position());
        break;
    case VertexType::POSITION_NORMAL:
        m_Material.SetPipeline(*MeshPipeline::PositionNormal());
    default:
        break;
    }
}
*/
